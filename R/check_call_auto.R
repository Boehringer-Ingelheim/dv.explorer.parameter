# Automatically generated module API check functions. Think twice before editing them manually.
({
# styler: off

# dv.explorer.parameter::mod_corr_hm
check_mod_corr_hm_auto <- function(afmm, datasets, module_id, bm_dataset_name, subjid_var,
    cat_var, par_var, visit_var, value_vars, default_cat, default_par,
    default_visit, default_val) {
    NULL
}

# dv.explorer.parameter::mod_lineplot
check_mod_lineplot_auto <- function(afmm, datasets, module_id, bm_dataset_name, group_dataset_name,
    receiver_id, subjid_var, cat_var, par_var, visit_vars, cdisc_visit_vars,
    value_vars, additional_listing_vars, ref_line_vars, default_transparency) {
    NULL
}

})
# styler: on
