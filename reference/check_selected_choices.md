# If selected is not contained in choices send a warning

If selected is not contained in choices send a warning

## Usage

``` r
check_selected_choices(selected, choices, inputId)
```

## Arguments

- selected:

  selection set

- choices:

  choices set

- inputId:

  the inputId of the shiny element being checked
