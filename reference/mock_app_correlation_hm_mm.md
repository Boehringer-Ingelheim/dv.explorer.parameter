# Mock corr hm app

Mock corr hm app

## Usage

``` r
mock_app_correlation_hm_mm(anlfl_flags = FALSE)
```

## Arguments

- anlfl_flags:

  indicates that the input data contain analysis flag variables or not
